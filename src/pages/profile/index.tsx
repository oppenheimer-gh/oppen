import { ProfileModule } from "@/components/modules";
import { NextPage } from "next";
import React from "react";

const ProfilePage: NextPage = () => {
  return <ProfileModule />;
};

export default ProfilePage;
