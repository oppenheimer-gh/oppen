import { RegisterModule } from "@/components/modules";
import { NextPage } from "next";
import React from "react";

const RegisterPage: NextPage = () => {
  return <RegisterModule />;
};

export default RegisterPage;
